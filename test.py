# __add__ method
#uuid

import csv
    #print(line['time'], line['lat'], line['lon'], line['model'], line['id'],line['rssi'], line['code']) 


test_id = {'156870': {
  'count': 1,
  'times': ['']
}}

print(test_id)

class UID:
  def __init__(self, id):